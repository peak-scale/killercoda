---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: sops-encrypted
spec:
  interval: 1m
  path: "./playgrounds/sops-secrets/example"
  sourceRef:
    kind: GitRepository
    name: sops-encrypted
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg